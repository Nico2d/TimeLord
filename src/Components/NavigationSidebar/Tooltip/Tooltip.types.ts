import React from "react";

export type TooltipProps = {
  text: string;
  children: React.ReactNode;
};

export type PositionTypes = {
  x: number;
  y: number;
  width: number;
  height: number;
};
